import React from 'react'
import { Text, View } from "react-native"

const TesteStorage = ()=>{
    const dados = ['s','d','e']
    return(
        <View>
            <Text>Testando</Text>
        </View>
    )
}

export default TesteStorage;